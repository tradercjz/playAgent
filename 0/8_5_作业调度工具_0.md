Prompt:
在DolphinDB中，当任务间不存在复杂依赖关系时，推荐使用哪个内置函数进行作业调度？
Input:
Answer:


在DolphinDB中，当任务间不存在复杂依赖关系时，推荐使用内置函数`scheduleJob`进行作业调度。该函数适用于简单任务编排场景，能够满足无复杂依赖关系的作业调度需求。

Input:
Function Name:
scheduleJob
R1 CoT:
好的，我需要根据用户提供的文档来回答问题。用户的问题是：“在DolphinDB中，当任务间不存在复杂依赖关系时，推荐使用哪个内置函数进行作业调度？”

首先，我要仔细阅读提供的文档内容。文档中提到，当任务较为简单且没有依赖关系时，DolphinDB内置的scheduleJob功能可以用于编排作业。而对于复杂依赖的情况，则推荐使用Airflow或DolphinScheduler。

接下来，我需要确认问题中的关键词。用户提到的是“内置函数”，而文档中提到的scheduleJob功能是DolphinDB内置的，符合条件。因此，答案应该是scheduleJob这个函数。

需要确保没有混淆其他工具，比如Airflow或DolphinScheduler，但这两个是外部工具，不是内置函数。所以正确答案就是scheduleJob。

Correct CoT:
好的，我需要根据用户提供的文档来回答问题。用户的问题是：“在DolphinDB中，当任务间不存在复杂依赖关系时，推荐使用哪个内置函数进行作业调度？”

首先，我要仔细阅读提供的文档内容。文档中提到，当任务较为简单且没有依赖关系时，DolphinDB内置的scheduleJob功能可以用于编排作业。而对于复杂依赖的情况，则推荐使用Airflow或DolphinScheduler。

接下来，我需要确认问题中的关键词。用户提到的是“内置函数”，而文档中提到的scheduleJob功能是DolphinDB内置的，符合条件。因此，答案应该是scheduleJob这个函数。

需要确保没有混淆其他工具，比如Airflow或DolphinScheduler，但这两个是外部工具，不是内置函数。所以正确答案就是scheduleJob。

Function Name:
scheduleJob
