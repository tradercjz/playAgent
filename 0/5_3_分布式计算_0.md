Prompt:
DolphinDB的分布式计算框架支持哪些内置的计算模型？这些模型如何与分布式数据库的分区机制协同工作？
Input:
Answer:


DolphinDB的分布式计算框架支持以下三种内置计算模型：
1. Pipeline模型
2. Map-reduce模型
3. 迭代的map-reduce模型

这些模型与分布式数据库的分区机制协同工作的方式是：系统以分区为最小计算单元，既支持将计算逻辑直接下推到数据所在的物理节点进行本地化计算（减少数据移动），也支持根据需要在计算节点之间传输特定分区的数据。这种与分布式存储紧密结合的架构，使得计算可以最大程度地利用数据本地性优势，同时保持计算粒度的灵活性。
R1 CoT:
好的，我需要回答用户关于DolphinDB分布式计算框架支持的内置计算模型以及它们如何与分区机制协同工作的问题。

首先，回顾提供的文档内容。文档提到DolphinDB的分布式计算框架以分区为粒度，与分布式数据库紧密集成，内置了pipeline、map-reduce和迭代的map-reduce等模型。接下来需要将这些信息整理成答案。

确定问题两部分：支持哪些模型，以及它们如何协同工作。文档明确列出了三个模型：pipeline、map-reduce和迭代的map-reduce。在协同工作方面，需要说明这些模型如何利用分区机制，比如计算下推到数据节点，减少数据传输，或者根据需要进行数据传输到计算节点。

确保回答准确，不添加文档未提及的信息。使用简洁明了的语言，分点回答每个部分。检查是否有遗漏或误解，比如确认是否还有其他模型未被提及，但文档只提到这三个，所以应严格按此回答。

最后，组织语言，确保逻辑连贯，先列出模型，再解释协同机制。可能需要合并类似点，比如pipeline和map-reduce都涉及数据本地化计算，而迭代模型可能用于多次处理。同时注意术语的正确使用，如“计算下推”等。

Correct CoT:
好的，我需要回答用户关于DolphinDB分布式计算框架支持的内置计算模型以及它们如何与分区机制协同工作的问题。

首先，回顾提供的文档内容。文档提到DolphinDB的分布式计算框架以分区为粒度，与分布式数据库紧密集成，内置了pipeline、map-reduce和迭代的map-reduce等模型。接下来需要将这些信息整理成答案。

确定问题两部分：支持哪些模型，以及它们如何协同工作。文档明确列出了三个模型：pipeline、map-reduce和迭代的map-reduce。在协同工作方面，需要说明这些模型如何利用分区机制，比如计算下推到数据节点，减少数据传输，或者根据需要进行数据传输到计算节点。

确保回答准确，不添加文档未提及的信息。使用简洁明了的语言，分点回答每个部分。检查是否有遗漏或误解，比如确认是否还有其他模型未被提及，但文档只提到这三个，所以应严格按此回答。

最后，组织语言，确保逻辑连贯，先列出模型，再解释协同机制。可能需要合并类似点，比如pipeline和map-reduce都涉及数据本地化计算，而迭代模型可能用于多次处理。同时注意术语的正确使用，如“计算下推”等。

Function Name:
mr (map-reduce相关函数)
