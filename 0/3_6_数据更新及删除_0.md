Prompt:
在DolphinDB中使用TSDB引擎建表时，如何配置参数以实现软删除功能？
Input:
Answer:


在DolphinDB中使用TSDB引擎建表时，需在建表语句中设置参数`softDelete=true`即可启用软删除功能。该参数会将删除操作标记为逻辑删除，实际数据文件在后续合并时才会物理删除，从而提升高频删除场景的性能表现。
R1 CoT:
好的，我需要回答用户的问题：“在DolphinDB中使用TSDB引擎建表时，如何配置参数以实现软删除功能？”首先，我需要回顾提供的文档内容，找到与软删除相关的部分。

根据文档的“数据删除”章节，当使用TSDB引擎并采用保留最新数据的机制时，可以通过设置softDelete参数来启用软删除。具体来说，建表时设置softDelete=true即可启用软删除功能。此时，删除操作会将数据标记为删除，而不是立即物理删除，这提高了删除性能。而如果设置为false，则会采用类似OLAP引擎的删除流程，需要读取整个分区数据并重写，效率较低。

因此，正确的做法是在建表语句中指定softDelete=true。例如，使用createTable函数的参数配置或者在建表时显式设置。需要确认用户是否需要具体的代码示例，但根据问题，用户只需要知道如何配置参数，所以直接指出设置softDelete为true即可。

Correct CoT:
好的，我需要回答用户的问题：“在DolphinDB中使用TSDB引擎建表时，如何配置参数以实现软删除功能？”首先，我需要回顾提供的文档内容，找到与软删除相关的部分。

根据文档的“数据删除”章节，当使用TSDB引擎并采用保留最新数据的机制时，可以通过设置softDelete参数来启用软删除。具体来说，建表时设置softDelete=true即可启用软删除功能。此时，删除操作会将数据标记为删除，而不是立即物理删除，这提高了删除性能。而如果设置为false，则会采用类似OLAP引擎的删除流程，需要读取整个分区数据并重写，效率较低。

因此，正确的做法是在建表语句中指定softDelete=true。例如，使用createTable函数的参数配置或者在建表时显式设置。需要确认用户是否需要具体的代码示例，但根据问题，用户只需要知道如何配置参数，所以直接指出设置softDelete为true即可。

Function Name:
createPartitionedTable
