Prompt:
DolphinDB的分布式计算引擎内置了哪些计算框架来适应不同的计算场景？
Input:
Answer:


DolphinDB的分布式计算引擎内置了流水线、Map-Reduce和迭代计算等多种计算框架来适应不同的计算场景。
R1 CoT:
好的，我现在需要回答用户的问题：“DolphinDB的分布式计算引擎内置了哪些计算框架来适应不同的计算场景？”首先，我得仔细查看用户提供的文档内容，找到相关的部分。

根据文档，在“体系架构”部分提到分布式计算引擎内置了流水线、Map-Reduce和迭代计算等多种计算框架。其中Map-Reduce是核心功能，用于处理大数据集。用户的问题正是询问这些内置的计算框架，所以答案应该包括这三个：流水线、Map-Reduce和迭代计算。

我需要确认是否还有其他框架被提及。文档中还提到用户可以利用这些模型进行高效并行处理，但未列出其他框架名称。因此，答案应明确列出这三个框架。同时，可能需要补充说明它们各自的作用，比如流水线处理数据流，Map-Reduce处理分布式任务，迭代计算适用于需要多次迭代的场景。但用户的问题仅要求列出名称，所以保持简洁即可。

Correct CoT:
好的，我现在需要回答用户的问题：“DolphinDB的分布式计算引擎内置了哪些计算框架来适应不同的计算场景？”首先，我得仔细查看用户提供的文档内容，找到相关的部分。

根据文档，在“体系架构”部分提到分布式计算引擎内置了流水线、Map-Reduce和迭代计算等多种计算框架。其中Map-Reduce是核心功能，用于处理大数据集。用户的问题正是询问这些内置的计算框架，所以答案应该包括这三个：流水线、Map-Reduce和迭代计算。

我需要确认是否还有其他框架被提及。文档中还提到用户可以利用这些模型进行高效并行处理，但未列出其他框架名称。因此，答案应明确列出这三个框架。同时，可能需要补充说明它们各自的作用，比如流水线处理数据流，Map-Reduce处理分布式任务，迭代计算适用于需要多次迭代的场景。但用户的问题仅要求列出名称，所以保持简洁即可。

Function Name:

