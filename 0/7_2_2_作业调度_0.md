Prompt:
如何手动设置DolphinDB中批处理作业的优先级？
Input:
Answer:


要手动设置DolphinDB中批处理作业的优先级，可以使用`submitJobEx`函数并指定优先级参数。具体而言，系统默认通过`submitJob`提交的批处理作业优先级为4，而`submitJobEx`允许用户自定义作业优先级，从而实现手动设置。

Input:
Function Name:
submitJobEx
R1 CoT:
好的，我需要回答用户的问题：“如何手动设置DolphinDB中批处理作业的优先级？”根据提供的文档内容，手动设置批处理作业的优先级涉及到特定的函数。

首先，文档中提到，对于通过submitJob提交的批处理作业，默认优先级是4。但用户可以通过submitJobEx函数来指定优先级。此外，其他方法如setMaxJobPriority函数可能影响同步作业的优先级，但批处理作业需要用到submitJobEx。因此，正确的做法应该是使用submitJobEx函数，并在调用时指定优先级参数。需要确认函数名称是否正确，文档中确实提到了submitJobEx，所以答案应该是使用这个函数。因此，回答应该直接指出使用submitJobEx函数来手动设置批处理作业的优先级。

Correct CoT:
好的，我需要回答用户的问题：“如何手动设置DolphinDB中批处理作业的优先级？”根据提供的文档内容，手动设置批处理作业的优先级涉及到特定的函数。

首先，文档中提到，对于通过submitJob提交的批处理作业，默认优先级是4。但用户可以通过submitJobEx函数来指定优先级。此外，其他方法如setMaxJobPriority函数可能影响同步作业的优先级，但批处理作业需要用到submitJobEx。因此，正确的做法应该是使用submitJobEx函数，并在调用时指定优先级参数。需要确认函数名称是否正确，文档中确实提到了submitJobEx，所以答案应该是使用这个函数。因此，回答应该直接指出使用submitJobEx函数来手动设置批处理作业的优先级。

Function Name:
submitJobEx
