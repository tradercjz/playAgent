Prompt:
在DolphinDB 3.00.2版本中，如何在策略中使用新增的onTimer回调函数？
Input:
Answer:


在DolphinDB 3.00.2版本中，使用新增的`onTimer`回调函数需要先在策略脚本中定义该函数，并通过`createBacktester`接口创建回测引擎后自动注册。该回调函数会按照预设的时间间隔触发，常用于执行定时任务，例如：

```python
def onTimer(engine, indicator, timestamp){
    // 定时执行交易逻辑，如获取持仓、提交订单等
    positions = engine.getPosition()
    engine.submitOrder(symbol, direction, quantity)
}

// 创建回测引擎时设置定时器间隔(单位：毫秒)
engine = createBacktester(
    strategy="myStrategy",
    mode="market",
    timerInterval=5000  // 每5秒触发一次onTimer
)
```

注意需通过`setBacktestMode`选择JIT模式或事件驱动模式，并确保在`context`配置中声明需要访问的引擎数据。建议结合`subscribeIndicator`订阅指标计算结果，实现定时策略与行情联动的自动化交易。
R1 CoT:
好的，我需要回答用户关于DolphinDB 3.00.2版本中如何使用新增的onTimer回调函数的问题。首先，我要回想文档中的相关内容。文档中提到，在版本3.00.2.82.00.14.8中新增支持了onTimer回调函数。

用户的问题是具体如何在策略中使用这个函数。由于文档没有详细说明用法，可能需要根据DolphinDB的一般回调函数使用方法来推断。通常，回调函数需要注册并在特定条件下触发。例如，在定时器回调中，可能需要设置定时器的时间间隔，并定义onTimer函数来处理定时触发的事件。

可能的步骤包括：创建回测引擎、定义onTimer函数来执行周期性任务、设置定时器的间隔时间，并将该函数注册到引擎中。但需要确认是否有特定的配置项或API来启用或设置定时器参数。此外，文档提到了新增的引擎配置项如context和addTimeColumnInIndicator，可能相关。

需要注意的是，文档中提到的createBacktester和setBacktestMode等接口可能与此有关。用户可能需要使用这些接口来配置回测模式，并在策略脚本中定义onTimer函数的具体逻辑，比如定期执行某些计算或提交订单。

总结回答时，应指出在创建回测引擎后，通过定义onTimer函数并设置定时器参数（如间隔时间），然后在引擎配置中启用该回调。可能需要参考官方文档或示例代码来确保正确使用。

Correct CoT:
好的，我需要回答用户关于DolphinDB 3.00.2版本中如何使用新增的onTimer回调函数的问题。首先，我要回想文档中的相关内容。文档中提到，在版本3.00.2.82.00.14.8中新增支持了onTimer回调函数。

用户的问题是具体如何在策略中使用这个函数。由于文档没有详细说明用法，可能需要根据DolphinDB的一般回调函数使用方法来推断。通常，回调函数需要注册并在特定条件下触发。例如，在定时器回调中，可能需要设置定时器的时间间隔，并定义onTimer函数来处理定时触发的事件。

可能的步骤包括：创建回测引擎、定义onTimer函数来执行周期性任务、设置定时器的间隔时间，并将该函数注册到引擎中。但需要确认是否有特定的配置项或API来启用或设置定时器参数。此外，文档提到了新增的引擎配置项如context和addTimeColumnInIndicator，可能相关。

需要注意的是，文档中提到的createBacktester和setBacktestMode等接口可能与此有关。用户可能需要使用这些接口来配置回测模式，并在策略脚本中定义onTimer函数的具体逻辑，比如定期执行某些计算或提交订单。

总结回答时，应指出在创建回测引擎后，通过定义onTimer函数并设置定时器参数（如间隔时间），然后在引擎配置中启用该回调。可能需要参考官方文档或示例代码来确保正确使用。

Function Name:
onTimer

Prompt:
DolphinDB回测引擎中getAvailableCash接口返回资金不准确应如何解决？
Input:
Function Name:
getAvailableCash

Prompt:
如何在DolphinDB 3.00.2版本中创建回测引擎实例？
Input:
Function Name:
createBacktester

Prompt:
在期货回测中如何正确获取带有symbolSource字段的订单信息？
Input:
Function Name:
onOrder

Prompt:
DolphinDB回测引擎中如何设置不同的回测模式？
Input:
Function Name:
setBacktestMode
