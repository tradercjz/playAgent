Prompt:
DolphinDB中常用于元编程的函数有哪些？
Input:
Answer:


DolphinDB中常用于元编程的函数包括：objByName、sqlCol、sqlColAlias、sql、expr、eval、partial和makeCall。
R1 CoT:
好的，我现在要回答用户的问题：“DolphinDB中常用于元编程的函数有哪些？”根据提供的文档，我需要回忆并提取相关信息。

首先，文档中提到了DolphinDB支持两种元编程方法，一种是使用尖括号<>，另一种是使用函数。然后列举了常用的函数，包括objByName、sqlCol、sqlColAlias、sql、expr、eval、partial、makeCall等。这些函数的具体用途可能在文档的其他部分有详细说明，但当前问题只需要列出函数名称。

我需要确保答案准确无误，不遗漏任何提到的函数。检查原文中的列表：objByName、sqlCol、sqlColAlias、sql、expr、eval、partial、makeCall。确认这些都是文档中明确提到的，没有添加其他未提及的函数。此外，注意是否有拼写错误，比如sqlCol和sqlColAlias的大小写是否正确，是否需要用逗号分隔等。最后，将答案整理成清晰的形式，用中文列出所有函数名。

Correct CoT:
好的，我现在要回答用户的问题：“DolphinDB中常用于元编程的函数有哪些？”根据提供的文档，我需要回忆并提取相关信息。

首先，文档中提到了DolphinDB支持两种元编程方法，一种是使用尖括号<>，另一种是使用函数。然后列举了常用的函数，包括objByName、sqlCol、sqlColAlias、sql、expr、eval、partial、makeCall等。这些函数的具体用途可能在文档的其他部分有详细说明，但当前问题只需要列出函数名称。

我需要确保答案准确无误，不遗漏任何提到的函数。检查原文中的列表：objByName、sqlCol、sqlColAlias、sql、expr、eval、partial、makeCall。确认这些都是文档中明确提到的，没有添加其他未提及的函数。此外，注意是否有拼写错误，比如sqlCol和sqlColAlias的大小写是否正确，是否需要用逗号分隔等。最后，将答案整理成清晰的形式，用中文列出所有函数名。

Function Name:
objByName, sqlCol, sqlColAlias, sql, expr, eval, partial, makeCall
